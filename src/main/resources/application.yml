spring:
  datasource:
    url: jdbc:h2:mem:diarydb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update

diary:
  repo-path: ./diary-repo